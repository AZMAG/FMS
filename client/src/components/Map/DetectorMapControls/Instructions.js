import React from "react";

export default function Instructions() {
    return (
        <div className="bg-mag-teal/25 rounded-lg py-2 px-3 text-base text-mag-teal">
            Use the dropdown options below to change the detector data to be
            shown on the map.
        </div>
    );
}

import React from "react";

export default function Instructions() {
    return (
        <div className="bg-blue-100 rounded-lg py-2 px-3 text-base text-blue-700">
            Use the dropdown options below to change the detector data to be
            shown on the map.
        </div>
    );
}
